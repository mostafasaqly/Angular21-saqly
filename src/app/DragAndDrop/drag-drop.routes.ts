import { Routes } from '@angular/router';
import { DragDropDemoComponent } from './drag-drop-demo.component';

export const DRAG_DROP_ROUTES: Routes = [
  { path: 'drag-drop', component: DragDropDemoComponent },
];
